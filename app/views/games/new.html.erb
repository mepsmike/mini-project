

<%= form_for @game, :url => games_path do |f| %>
	  <% if @game.errors.any? %>
    				<div id="error_explanation">
      					<h2><%= pluralize(@game.errors.count, "error") %> prohibited this person from being saved:</h2>

      					<ul>
      					<% @game.errors.full_messages.each do |message| %>
       						<li><%= message %></li>
      					<% end %>
      					</ul>
   					 </div>
  				<% end %>
	<%= f.label :name, "name"%>
	<%= f.text_field :name%>

	<%= f.label :year, "year"%>
	<%= f.text_field :year%>

	<%= f.label :description, "description"%>
	<%= f.text_area :description%>

	<%= f.submit "Create" %>

<% end %>